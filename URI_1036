# -*- coding: utf-8 -*-

import math
A, B, C= input().split(' ')

delta= float(B)**2 - 4* float(A) * float(C)
if float(A)==0:
    print ('Impossivel calcular')
elif delta>=0:
    R1= (-float(B) +math.sqrt(delta))/(2*float(A))
    R2= (-float(B) -math.sqrt(delta))/(2*float(A))
    print('R1 = %.5f' %R1)
    print('R2 = %.5f' %R2)

else:
    print ('Impossivel calcular')
